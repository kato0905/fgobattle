<%= form_with(model: skill, local: true) do |form| %>
  <% if skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(skill.errors.count, "error") %> prohibited this skill from being saved:</h2>

      <ul>
      <% skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :servant_id %>
    <%= form.number_field :servant_id, id: :skill_servant_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :skill_name %>
  </div>

  <div class="field">
    <%= form.label :explain %>
    <%= form.text_field :explain, id: :skill_explain %>
  </div>

  <div class="field">
    <%= form.label :d_pattern %>
    <%= form.number_field :d_pattern, id: :skill_d_pattern %>
  </div>

  <div class="field">
    <%= form.label :detail %>
    <%= form.text_field :detail, id: :skill_detail %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
