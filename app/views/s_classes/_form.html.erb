<%= form_with(model: s_class, local: true) do |form| %>
  <% if s_class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(s_class.errors.count, "error") %> prohibited this s_class from being saved:</h2>

      <ul>
      <% s_class.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :class_id %>
    <%= form.number_field :class_id, id: :s_class_class_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :s_class_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
